{
  "name": "weather-backend",
  "version": "1.0.0",
  "description": "Backend service for fetching weather data from WeatherAPI.com",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "dependencies": {
    "axios": "^1.1.2",
    "cors": "^2.8.5",
    "express": "^4.1.0",
    "express-rate-limit": "^5.1.0",
    "moment": "^2.30.1"
  }
}
